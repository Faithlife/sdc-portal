var context = require('../context');

context.createAction('ui:activeUser:select', function (params) {
  return params;
});

context.createAction('ui:activeDataCenter:select', function (params) {
  return params;
});
